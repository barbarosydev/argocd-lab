apiVersion: v2
name: airflow
description: Apache Airflow with external PostgreSQL
type: application
version: 1.0.0
# Versions configured via LAB_AIRFLOW_VERSION and LAB_AIRFLOW_HELM_VERSION in .env
appVersion: "3.0.2"
dependencies:
  - name: airflow
    version: 1.18.0
    repository: https://airflow.apache.org
