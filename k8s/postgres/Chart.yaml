apiVersion: v2
name: postgres
description: PostgreSQL database for Airflow
type: application
version: 1.0.0
# Versions configured via LAB_POSTGRES_VERSION and LAB_POSTGRES_HELM_VERSION in .env
appVersion: "17.6.0"
dependencies:
  - name: postgresql
    version: 16.7.27
    repository: https://charts.bitnami.com/bitnami
