version: '3'

tasks:
  deploy:
    desc: "🐘 Deploy PostgreSQL database"
    silent: true
    deps:
      - task: :_check-cluster
    cmds:
      - ./scripts/postgres.sh deploy

  undeploy:
    desc: "🐘 Undeploy PostgreSQL database"
    silent: true
    deps:
      - task: :_check-cluster
    cmds:
      - ./scripts/postgres.sh undeploy

  status:
    desc: "🐘 Check PostgreSQL deployment status"
    silent: true
    deps:
      - task: :_check-cluster
    cmds:
      - ./scripts/postgres.sh status

  password:
    desc: "🐘 Show PostgreSQL credentials"
    silent: true
    deps:
      - task: :_check-cluster
    cmds:
      - ./scripts/postgres.sh password

  shell:
    desc: "🐘 Open psql shell to PostgreSQL"
    silent: true
    deps:
      - task: :_check-cluster
    cmds:
      - ./scripts/postgres.sh shell
